/**
 * Example utility function for demonstration purposes.
 * This can be deleted once real utilities are implemented.
 */
export function sum(a: number, b: number): number {
  return a + b;
}

export function multiply(a: number, b: number): number {
  return a * b;
}
